import { ChartLimitPipe } from './chart-limit.pipe';

describe('ChartLimitPipe', () => {
  it('create an instance', () => {
    const pipe = new ChartLimitPipe();
    expect(pipe).toBeTruthy();
  });
});
